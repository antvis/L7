import BaseLayer from '../layer/baseLayer';
describe('baseLayer', () => {
  it('set option', () => {
    const option = {
      adcode: [],
    };
    // const layer = new BaseLayer(null, option);
  });
});
