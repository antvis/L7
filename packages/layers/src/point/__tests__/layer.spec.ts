import PointLayer from '../';
import Map from '../../../../maps/src/map';
import { Scene } from '../../../../scene/src';
describe('pointLayer', () => {
  it('init', () => {
    expect(2).toEqual(2);
  });
});
