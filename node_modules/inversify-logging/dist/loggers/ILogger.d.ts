export declare enum LogLevel {
    Debug = 0,
    Info = 1,
    Warning = 2,
    Error = 3,
}
export interface ILogger {
    debug(...messages: string[]): any;
    info(...messages: string[]): any;
    warning(...messages: string[]): any;
    error(...errors: (string | Error)[]): any;
    setLogLevel?(level: LogLevel): any;
    createChildLogger(context: string): ILogger;
}
export interface ILoggerConfig {
    logLevel: LogLevel;
}
export declare class DefaultLoggerConfig implements ILoggerConfig {
    logLevel: LogLevel;
}
