"use strict";

exports.onPreInit = function (_ref, options) {
  var reporter = _ref.reporter;

  if (!options.trackingId) {
    reporter.warn("The Google Analytics plugin requires a tracking ID. Did you mean to add it?");
  }
};