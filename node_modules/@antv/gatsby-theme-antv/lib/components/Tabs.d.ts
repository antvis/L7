import React from 'react';
declare const Tabs: React.FC<{
    active: 'examples' | 'API' | 'design';
    slug: string;
    showTabs: {
        examples?: boolean;
        API?: boolean;
        design?: boolean;
    };
}>;
export default Tabs;
